apiVersion: apps/v1
kind: Deployment
metadata:
  name: pdf-chat-app
  labels:
    app: pdf-chat-app
    rel: stable
    costCenter: B0012C

spec:
  replicas: 2
  minReadySeconds: 10
  selector:
    matchLabels:
      app: pdf-chat-app
  template:
    metadata:
      labels:
        app: pdf-chat-app
    spec:
      containers:
      - name: pdf-chat-app
        envFrom:
          - configMapRef:
              name: pdf-chat-app-configmap
        image: dataplat.azurecr.io/aoai/pdf-chat-app:05180f5e71e85918451cf773c0e9e61188493614
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8501
        resources:
          limits:
            memory: "512Mi"
            cpu: "250m"